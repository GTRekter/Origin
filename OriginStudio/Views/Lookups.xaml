<Page
    x:Class="OriginStudio.Views.Lookups"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OriginStudio"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid RequestedTheme="Dark" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <TextBlock Name="LoadingTextBlock"
                   Style="{StaticResource General.Views.Loading}" />

        <TextBlock Text="This is a complete list of all the lookups currently created. It's possible to edit or create a new lookup by click over the lookup name or over the Add button."/>

        <ListBox Name="LookupListBox" 
                 Style="{StaticResource Lookups.ListBox.LookupList}"
                 SelectionChanged="LookupListBox_SelectionChanged">
        </ListBox>

        <Button Name="AddLookupButton"
                Style="{StaticResource Lookups.Button.AddLookup}"
                Click="AddLookupButton_Click"/>

    </Grid>
</Page>
